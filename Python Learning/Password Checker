username = input('What is your Username?')
password = input('Type your Password-')
password_length = len(password)
hidden_password = '*' * password_length

print(f'{username}, your password is correct')
